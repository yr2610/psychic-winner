// 出力ファイル（Excel）名
// 現状ファイル名に明確なルールはないようなので
//【yymmdd更新】[型式]-[UPDATE日]
// のようにしています
[outputFilename]: 【yymmdd更新】XYZ-20200101

// index シートのシート名を変更したい場合
//[indexSheetname]: 表紙


// ==== index シート内のセルへの埋め込み値 ====

// index シート内の {{*}} セルに文字列を埋め込む

[TITLE]: XYZ/超絶面白ゲー（yy/mm/dd アップデート版）

// バージョン
// ドロップダウンリスト内の「対象外」で使用
[VERSION]: 20yy/mm/ddアップデート版

// 作成者
[AUTHOR]: 山田太郎

[H_ITEM]: 確認項目

// revision（初期値）
// 変数名の1文字目が _ で2文字目がアルファベット大文字は入力を取り込む
[_VERSION_MAIN]: Rev.0000
[_VERSION_STATION]: Rev.0000

// 確認者
[_CONFIRMOR]: 山田花子

// テスト開始日
// この日より前に確認したセルを暗い色で表示する
// 同じファイルを複数のバージョンのテストで引き続き使う場合に使う想定
[START_DATE]: 2020/01/01

// テスト実施者（初期値）
[_TESTER]: 山田次郎


// ==== 入力欄の初期値設定 ====

// 入力欄の順序の宣言
// estimated_time にはデフォルト値として 1 を設定
// シートで宣言することも可能（グローバルよりもシートのものが優先される）
((estimated_time: 1, result))

// 条件付きデフォルト値
// 正規表現にマッチする場合はここで指定した値を初期値とする
// 複数指定も可能
// 優先順位は記述順（先に書く程優先度が高い）
// シートで宣言することも可能（グローバルよりもシートのものが優先される）
// この例では行頭が【*】, ※※のいずれかの場合は estimated_time を空欄、 result を "-" とする
/^(【.+】|※※)/ ("", -)

// 初期値の優先順位は デフォルト値 < 条件付きデフォルト値 < ノードの指定 となる
